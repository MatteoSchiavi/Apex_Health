# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environment & secrets — never committed (MASTER_SPEC §22.5)
.env

# Local artifacts
*.db
*.sqlite3

# Database backups — encrypted artifacts of REAL HEALTH DATA (§22.7).
# Never commit, never push. Restore tooling lives in backend/tools/.
backups/

# OS / editor
.DS_Store
.idea/
.vscode/
