"""Agent harness package (§8).

Phase 3 ships the entrypoint the bot calls for free text — a real,
data-grounded completion with chat logging. Phase 5 (§23) lands the full
harness here: tool registry, tool loop, routing, token accounting."""
